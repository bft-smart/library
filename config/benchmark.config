#######################
# Required parameters #
#######################
#IP address where the controller will listen for workers' connections
controller.listening.ip = <controller's ip address>

#Port number where the controller will listen for workers' connections
controller.listening.port = <controller's listening port>

#Number of machines
global.worker.machines = <number of worker machines>

#Controller benchmark strategy class implementing IBenchmarkStrategy
controller.benchmark.strategy = bftsmart.benchmark.BenchmarkStrategy

#Worker setup class implementing ISetupWorker
controller.worker.setup = bftsmart.benchmark.BenchmarkSetup

#Worker event processor class implementing IWorkerEventProcessor
controller.worker.processor = bftsmart.benchmark.BenchmarkEventProcessor

#############################################################################
# Extra parameters provided in IBenchmarkStrategy through Properties object #
#############################################################################
experiment.f = 1
experiment.hosts.file = hosts_f_1.config
experiment.clients_per_round = 1 50 100 500 1000 1500 2000
experiment.measure_resources = true
experiment.is_write = true
experiment.use_hashed_response = true
experiment.request_data_size = 0
experiment.response_data_size = 1024